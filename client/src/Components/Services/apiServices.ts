/* eslint-disable @typescript-eslint/explicit-function-return-type */
import axios, { type AxiosResponse } from 'axios';
// import { Service } from './types/servicesType';

// eslint-disable-next-line import/prefer-default-export
// export const loadServicesFetch = async (obj: Service): Promise<Service> => {
//   const response: AxiosResponse<{ service: Service }> = await axios.post(`/api/services/load`, obj);
//   return response.data.service;
// };

// export const logFetch = async (obj: UserInForm): Promise<User> => {
//   const res: AxiosResponse<{ message: string; user: User }> = await axios.post(`/api/sign/in`, obj);
//   return res.data.user;
// };
